# Environment maps

Drop `studio.hdr` (or `environment.hdr`) here. Recommended: 2K equirectangular
HDR from [Poly Haven](https://polyhaven.com/hdris).

If no file is present the demo falls back to a procedural `RoomEnvironment`.
